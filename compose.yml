services:
  basilasay:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: basilasay
    environment:
      - PORT=${PORT:-3000}
    networks:
      - caddy
    pull_policy: build
    restart: unless-stopped

networks:
  caddy:
    external: true
